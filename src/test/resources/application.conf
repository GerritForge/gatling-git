http {
  username: "admin",
  username: ${?GIT_HTTP_USERNAME},

  password: "secret",
  password: ${?GIT_HTTP_PASSWORD},
}

ssh {
  private_key_path: "/Users/syntonyze/.ssh/id_rsa",
  private_key_path: ${?GIT_SSH_PRIVATE_KEY_PATH},
}

tmpFiles {
  basePath: "/tmp"
  basePath: ${?TMP_BASE_PATH}
}
